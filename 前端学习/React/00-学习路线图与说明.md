# React 学习路线图与说明

> **文件编码**：本文件夹内所有 `.md` 均为 **UTF-8**。React 项目源文件建议 UTF-8，VS Code / Cursor 右下角确认编码；若中文乱码，点右下角编码 → **通过编码重新打开** → 选 UTF-8。

---

## 1. 这套资料适合谁

- 已学完或正在学 [HTML/CSS/JS](../HTML%20CSS%20JS/00-学习路线图与说明.md)（至少 06～09 章：DOM、事件、fetch、模块化）的同学
- 想从原生 JS 升级到 **React** 框架开发的学习者
- 已学过 [Vue 3](../Vue/00-学习路线图与说明.md) 想横向对比 React 的同学
- 计划与后端项目做**前后端联调**、投递全栈/前端岗位的同学（[Java / Spring Boot](../../后端学习/Java/00-学习路线图与说明.md) 或 [Python / FastAPI](../../后端学习/Python/00-学习路线图与说明.md)，**选一条后端主线**）
- 目标：能独立搭建 React 18 + Vite 项目、写函数组件与 Hooks、走路由、管状态、调接口、做一个能演示的**商城前台 MVP（shop-react）**

**不适合**：

- 已熟练 React + Redux 多年且做过大型 SPA 的资深开发者（可直接看 12、13、14 查漏补缺）
- 完全零基础、连 HTML 标签和 JS 变量都不熟（请先学 HTML CSS JS 01～05）

**前置要求（自检）**：

| 能力 | 对应 HTML CSS JS 章节 | 自检方式 |
|------|----------------------|----------|
| HTML 语义化结构 | 01～02 | 能写 header/nav/main/footer |
| Flex 布局 | CSS 03 | 能居中、两栏布局 |
| ES6+ 箭头函数、解构、模板字符串 | JS 06 | 能改写旧式 function |
| Promise / async-await | JS 08 | 能写 `await fetch(...)` |
| 模块化 import/export | JS 09 | 能拆 js 文件 |
| HTTP 基础、JSON | JS 08 + 后端 04 概念 | 知道 GET/POST、状态码 200/401 |
| TypeScript 基础（建议） | [TS 01～06](../TypeScript/00-学习路线图与说明.md) | 08 章联调前完成更佳 |

---

## 2. 技术栈主线（本资料默认路线）

```text
React 18 入门与环境（Vite + React 模板）
  → JSX 语法与函数组件（props / children / 条件与列表渲染）
  → State 与事件处理（useState / 受控表单 / 状态提升）
  → 组件通信与组件设计（单向数据流 / 组合 vs 继承）
  → Hooks 深入（useEffect / useRef / 自定义 Hook）
  → React Router 单页路由（动态路由 / 导航守卫）
  → Zustand 状态管理（userStore / cartStore）
  → Axios 网络请求与前后端联调（拦截器 / token）
  → Ant Design UI 工程化（Form / Table / Layout）
  → Vite 构建与项目部署（env / Nginx / Docker）
  → 项目实战 + 进阶特性 + 面试专题
```

与后端的关系（全栈视角）：

| React（本资料） | 后端（对应） | 联调要点 |
|-----------------|--------------|----------|
| Axios 调 `/api/users` | 04 Spring Boot `@GetMapping` | 统一 JSON 结构 |
| 登录存 token | JWT + 拦截器 | Header: `Authorization: Bearer xxx` |
| 商品列表分页 | 05 MyBatis + PageHelper | `pageNum` / `pageSize` query |
| Zustand 存用户信息 | Redis Session（可选） | 前端持久化 localStorage |
| 路由守卫鉴权 | 后端 401 处理 | 双端都要拦未登录 |
| Vite proxy / Nginx 反代 | 09 部署 + CORS | 生产同域 `/api` |
| npm run build → dist | jar + docker-compose | Nginx 同时托管前后端 |

与 Vue 路线的平行对照（便于横向迁移）：

| 能力 | Vue 资料 | React 资料 |
|------|----------|------------|
| 脚手架 | `npm create vue@latest` | `npm create vite@latest -- --template react` |
| 视图语法 | 模板 + 指令 | JSX |
| 响应式 | ref / reactive | useState / useReducer |
| 副作用 | watch / onMounted | useEffect |
| 路由 | Vue Router | React Router v6 |
| 全局状态 | Pinia | Zustand |
| UI 库 | Element Plus | Ant Design |
| 练手项目 | shop-vue | shop-react |

---

## 3. 学习顺序（按编号）

```text
00 学习路线图（你现在在这里）
 ↓
01 React 入门与环境搭建
 ↓
02 JSX 语法与组件基础
 ↓
03 State 与事件处理
 ↓
04 组件通信与组件设计
 ↓
05 Hooks 深入（useEffect / useRef / 自定义 Hook）
 ↓
06 React Router 路由管理
 ↓
07 Zustand 状态管理
 ↓
08 Axios 网络请求与前后端联调
 ↓
09 Ant Design 与 UI 工程化
 ↓
10 Vite 构建与项目部署
 ↓
11 React 项目实战与面试准备
 ↓
12 React 进阶特性
 ↓
13 高频场景题与面试专题
 ↓
14 补充知识点总表（复习索引）
```

### 3.0 阶段目标总览

| 阶段 | 文档 | 核心目标 | 产出物 |
|------|------|----------|--------|
| 入门 | 01~03 | 会创建项目、写 JSX、useState、表单 | 商品列表 + 搜索 + 登录表单 |
| 组件 | 04~05 | 会拆组件、通信、useEffect | ProductCard、SearchBar、useProducts |
| 工程 | 06~08 | 会路由、状态管理、调后端接口 | 多页面 SPA + 登录 + 接口数据 |
| UI/构建 | 09~10 | 组件库、打包部署 | Ant Design 界面 + dist 部署 |
| 实战 | 11 | 完整商城前台 MVP | 可演示 Git 仓库 + README |
| 进阶 | 12~13 | 性能、并发特性、面试表达 | 面试笔记 + 场景题答案 |
| 复习 | 14 | 查漏 | 自评表全部 ✅ |

---

## 3.1 各章衔接索引（上一章产出 → 本章解决什么）

| 编号 | 上一章学了什么 | 本章要解决什么 | 关键 API / 概念 |
|------|----------------|----------------|-----------------|
| 01 | 00 路线图：知道学什么、用什么工具 | 创建第一个 React 项目，看懂入口与 App 结构 | `create vite`、`npm run dev` |
| 02 | 01 能跑起来 | JSX 规则、函数组件、props、列表 key | `className`、条件渲染、map |
| 03 | 02 会展示数据 | useState、受控组件、状态提升 | 不可变更新、表单 onSubmit |
| 04 | 03 单文件臃肿 | 拆组件、props 回调、组合模式 | 单向数据流、children |
| 05 | 04 组件会拆 | useEffect、useRef、自定义 Hook | 依赖数组、清理函数 |
| 06 | 05 多页面切换难 | React Router 单页应用路由 | `Routes`、`Navigate`、loader |
| 07 | 06 跨页面共享状态 | Zustand 全局状态（用户、购物车） | `create`、`persist` |
| 08 | 07 有状态无数据 | Axios 调 Spring Boot 接口 | 拦截器、401、proxy |
| 09 | 08 接口通了 UI 丑 | Ant Design 快速搭界面 | `Form`、`Table`、`Layout` |
| 10 | 09 本地能跑 | `npm run build` 打包、Nginx 部署 | `.env`、`try_files` |
| 11 | 01～10 齐备 | 商城前台 MVP + 简历 + 面试话术 | 完整业务流程 |
| 12 | 11 项目能讲 | Suspense、memo、useMemo 等进阶 | 性能优化 |
| 13 | 12 技术点全 | 面试场景题、回答框架 | 原理 + 项目结合 |
| 14 | 01～13 过完 | 复习索引，逐项自评 | 总表勾选 |

---

## 3.2 shop-react 项目演进路线（01～11 共用同一个项目）

跟着文档做的话，建议始终维护一个叫 **`shop-react`** 的 React 项目，逐章叠加能力——与 Vue 侧 `shop-vue`、后端 `demo` 项目演进路线一一对应：

```text
01 章  npm create vite@latest shop-react -- --template react，跑通 Hello World
  ↓
02 章  商品列表页（map + props 假数据）
  ↓
03 章  搜索过滤（useState + 受控 input）+ 登录表单
  ↓
04 章  拆 ProductCard、SearchBar 组件
  ↓
05 章  抽 useProducts 自定义 Hook + useEffect 模拟请求
  ↓
06 章  + React Router（首页 / 列表 / 详情 / 登录 / 购物车）
  ↓
07 章  + Zustand（userStore、cartStore）
  ↓
08 章  + Axios 对接后端 demo 的 /api 接口
  ↓
09 章  + Ant Design 美化布局（Header、Table、Form）
  ↓
10 章  npm run build，Nginx 托管 dist + 反代 /api
  ↓
11 章  扩展为完整商城前台（注册、登录、商品、购物车、下单、订单列表）
```

各章「手把手」小节入口：

| 章节 | 入口小节 | 做什么 |
|------|----------|--------|
| 01 | §4.1 | Vite + React 逐步说明 |
| 02 | §15 | ProductList 完整组件 |
| 04 | §4.1 | ProductCard 从零拆组件 |
| 06 | §6.1 | 路由表 + 动态路由 :id |
| 08 | §8.1 | Axios 封装 + 拦截器 |
| 09 | §9.1 | Layout + 表格分页 |
| 10 | §8.1 | Nginx 完整配置 |
| 11 | §16.1 | 四周里程碑打勾 |

**Git 提交建议**（每章至少 1 次有意义 commit）：

```text
feat(ch02): add product list with map and key
feat(ch06): add react-router pages and guards
feat(ch08): integrate axios with spring boot api
feat(ch11): complete mall mvp with order flow
```

---

## 3.3 资料进度

| 编号 | 文件名 | 状态 | 重点内容 |
|------|--------|------|----------|
| 00 | 学习路线图与说明 | ✅ 已编写 | 衔接、环境、练习总表、里程碑 |
| 01 | React 入门与环境搭建 | ✅ 已编写 | Vite、JSX 初识、DevTools |
| 02 | JSX 语法与组件基础 | ✅ 已编写 | props、列表、条件渲染 |
| 03 | State 与事件处理 | ✅ 已编写 | useState、表单、状态提升 |
| 04 | 组件通信与组件设计 | 待编写 | props 回调、组合 |
| 05 | Hooks 深入 | 待编写 | useEffect、自定义 Hook |
| 06 | React Router | 待编写 | 守卫、懒加载 |
| 07 | Zustand | 待编写 | persist、中间件 |
| 08 | Axios 与联调 | 待编写 | 拦截器、proxy |
| 09 | Ant Design | 待编写 | Form/Table/Layout |
| 10 | Vite 构建部署 | 待编写 | env、Docker |
| 11 | 项目实战与面试 | 待编写 | MVP 规格 |
| 12 | React 进阶特性 | 待编写 | memo、Suspense |
| 13 | 场景题与面试 | 待编写 | 15+ 问答 |
| 14 | 补充知识点总表 | 待编写 | 全栈复习索引 |

---

## 4. 必备环境与工具

### 4.1 Node.js

- 推荐 **Node.js 18 LTS** 或 **20 LTS**（与 Vite 5、React 18 兼容良好）
- 不推荐 Node 16 以下（部分依赖可能报错）

验证：

```bash
node -v
# 预期输出：v18.x.x 或 v20.x.x

npm -v
# 预期输出：9.x.x 或 10.x.x
```

若 `node` 命令不存在：从 [nodejs.org](https://nodejs.org/) 下载 LTS 安装包，安装时勾选 **Add to PATH**。

### 4.2 包管理器

| 工具 | 说明 | 本资料默认 |
|------|------|------------|
| npm | Node 自带 | ✅ 默认 |
| pnpm | 更快、省磁盘 | 可选 `npm i -g pnpm` |
| yarn | 老牌替代 | 可选 |

```bash
# 可选：全局安装 pnpm
npm install -g pnpm
pnpm -v
```

### 4.3 编辑器与插件

- **VS Code** 或 **Cursor**（本资料示例以 VS Code 为准）
- **必装插件**：

| 插件 | 作用 | 注意 |
|------|------|------|
| ES7+ React/Redux/React-Native snippets | JSX 片段、快捷输入 | 可选但推荐 |
| ESLint | 代码规范 | 可选 |
| Prettier | 格式化 | 可选 |
| Error Lens | 行内报错 | 可选 |

**不需要** Vue 的 Volar 插件；React 用内置 JS/TS 语言服务 + ESLint 即可。

### 4.4 浏览器与调试

- Chrome / Edge 最新版
- 安装 **React Developer Tools** 扩展（Chrome 网上应用店搜索 React Developer Tools）
- 打开 DevTools → **Components** 面板，可查看组件树、props、hooks 状态

### 4.5 后端联调环境（08 章起需要）

| 组件 | 用途 | 启动方式 |
|------|------|----------|
| Spring Boot demo | REST API | IDEA Run 或 `java -jar` |
| MySQL | 数据 | Docker 见后端 06 章 |
| Postman / Apifox | 单独测接口 | 与前端 Network 对照 |

本地接口基址（开发）：`http://localhost:8080`  
前端 dev 服务器：`http://localhost:5173`（Vite 默认）

### 4.6 Git

与后端相同：从 **01 章** 创建 `shop-react` 当天就 `git init`，每完成一章至少 1 次 commit。

```bash
git init
git add .
git commit -m "chore: init shop-react with vite react template"
```

---

## 4.7 环境一键验证清单（学到对应章节前完成）

按顺序在终端执行，全部通过再进入对应章节。

**01 章前（Node + 编辑器）**：

```bash
node -v && npm -v
# 预期：版本号正常，无「不是内部或外部命令」

npm create vite@latest --help
# 预期：显示 create-vite 帮助信息
```

**01 章后（第一个项目能跑）**：

```bash
cd shop-react
npm install
npm run dev
# 预期：VITE v5.x ready in xxx ms
#        ➜  Local:   http://localhost:5173/
```

浏览器打开 5173，看到 Vite + React 欢迎页即 OK。

**06 章前（Router）**：

```bash
npm install react-router-dom
```

**07 章前（Zustand）**：

```bash
npm install zustand
```

**08 章前（Axios + 后端）**：

```bash
# 终端 1：后端
cd ../../后端学习/Java/demo   # 或你的 Spring Boot 路径
# IDEA Run DemoApplication 或 mvn spring-boot:run

# 终端 2：验证后端
curl http://localhost:8080/api/products
# 预期：JSON，含 code / data 字段（具体以后端为准）
```

**09 章前（Ant Design）**：

```bash
npm install antd
# 按需：npm install -D unplugin-auto-import（可选）
```

**10 章前（构建 + 可选 Docker）**：

```bash
npm run build
# 预期：dist/ 目录生成，无 ERROR

docker --version
# 09～10 章部署需要；无 Docker 也可先用本机 Nginx
```

---

## 5. 推荐学习四步法（每章都做）

1. **通读**：这章解决什么问题？和上一章什么关系？看 §「本章与上一章的关系」
2. **敲 demo**：`npm run dev` 跑起来，**完整手敲**文档代码（不要只复制不运行）
3. **做小练习**：章节末尾「分级练习」至少完成 **基础档**；04～08 建议完成进阶档
4. **复述**：合上书，用自己的话讲 5 条笔记，或录音 2 分钟

### 5.0 学习节奏建议（单日）

| 时间段 | 做什么 |
|--------|--------|
| 30 min | 通读 + 理解概念 |
| 60 min | 跟敲 demo |
| 30 min | 做练习 / 改 shop-react |
| 15 min | 写笔记 + git commit |

---

## 5.1 全路线分级练习总表

| 章节 | 基础 | 进阶 | 挑战 | 答案位置 |
|------|------|------|------|----------|
| 01 | Hello 改标题 | 加按钮计数器 | 拆成 Counter 子组件 | 01 篇 §练习 |
| 02 | 商品列表 map | 条件渲染库存标签 | 动态 className 促销样式 | 02 篇 §练习 |
| 03 | 登录表单受控 | 搜索过滤列表 | 状态提升到父组件 | 03 篇 §练习 |
| 04 | ProductCard 组件 | 父子传参展示价格 | 组合模式 Layout | 04 篇 §练习 |
| 05 | useEffect 拉假数据 | 抽 useProducts Hook | 防抖搜索 Hook | 05 篇 §练习 |
| 06 | 3 个路由页面切换 | 动态路由 `/products/:id` | ProtectedRoute 鉴权 | 06 篇 §练习 |
| 07 | userStore 登录态 | cartStore 加减算总价 | persist 持久化 | 07 篇 §练习 |
| 08 | GET 商品列表 | POST 登录带 token | 统一拦截 401 跳登录 | 08 篇 §练习 |
| 09 | Table 展示数据 | Form 校验登录 | Layout 后台布局 | 09 篇 §练习 |
| 10 | build 出 dist + preview | 配 .env.production | Nginx 托管 + 反代后端 | 10 篇 §8.1 |
| 11 | 走通登录→下单 | 写 README + 接口表 | 15 分钟项目串讲录音 | 11 篇 §16 |
| 12 | React.memo 优化 | lazy + Suspense | useMemo 大列表 | 12 篇 §10 |
| 13 | 每类题 2 分钟口述 | 结合 shop-react 举例 | 模拟面试 30 分钟 | 13 篇 §14 |

建议：**每章至少完成「基础」档**；06～08 三章的「进阶」档串起来就是可联调的 mini 商城。

---

## 5.2 里程碑检查表（按周，与 11 章对齐）

若每天学习 1.5～2 小时，可按下面打勾（详细 API 见 11 章）：

| 周 | 前端目标 | 后端配合（可选） | 验收 |
|----|----------|------------------|------|
| W1 | 路由 + Layout + 商品列表假数据 | — | 3 页面可切换 |
| W2 | Zustand + 登录页 + 路由守卫 | POST /api/login | 未登录进不了 /cart |
| W3 | Axios 对接 + Ant Design | GET /api/products | 列表来自接口 |
| W4 | 购物车 + 下单 + build 部署 | POST /api/orders | 浏览器完整流程 |

---

## 6. 学习时间参考（每天 1.5～2 小时）

| 文档 | 建议天数 | 说明 |
|------|----------|------|
| 01 | 3～5 天 | 环境、JSX 初识、第一个组件 |
| 02 | 3～5 天 | JSX 规则要多写 |
| 03 | 3～5 天 | useState 与表单是后面登录页基础 |
| 04 | 4～6 天 | 组件思维需要时间 |
| 05 | 4～6 天 | useEffect 与自定义 Hook |
| 06 | 4～6 天 | 路由 + 守卫 |
| 07 | 4～6 天 | Zustand 两个 store |
| 08 | 4～6 天 | 联调可能踩跨域坑 |
| 09 | 3～5 天 | 组件库查文档即可 |
| 10 | 3～5 天 | 部署第一次易白屏 |
| 11 | 10～14 天 | **综合项目**，别跳过 |
| 12 | 4～6 天 | 查漏补缺 |
| 13 | 持续 | 面试前反复过 |
| 14 | 1～2 天 | 自评总表 |

**全程约 2～3 个月**（含 shop-react 完整 MVP）。若同时学后端，建议 React 08 章与后端 04～05 章对齐进度。

---

## 7. 练手项目建议（11 章前后）

选一个主线做完，比泛泛看十遍文档有用。

### 方案 A：商城前台 shop-react（推荐，与后端 10 章配套）

- 用户注册 / 登录 / 退出
- 商品列表、详情、搜索
- 购物车（Zustand）
- 提交订单、订单列表
- Vite 打包 + Nginx 部署

### 方案 B：后台管理 admin-react（进阶）

- 登录 + 侧边栏 Layout
- 用户 / 商品 CRUD 表格
- 权限菜单（路由 loader / ProtectedRoute）
- 适合有实习方向「中后台」的同学

### 方案 C：待办 / 笔记 SPA（轻量）

- 与 HTML CSS JS 12 章待办联调
- 练 Router + Zustand + localStorage
- 可不依赖后端

**推荐目录结构（shop-react）**：

```text
shop-react/
├── public/
├── src/
│   ├── api/              # request.js, auth.js, product.js, order.js
│   ├── assets/
│   ├── components/       # AppHeader, ProductCard, EmptyState
│   ├── hooks/            # useProducts, useAuth
│   ├── pages/            # Home, Login, ProductList, ProductDetail, Cart, OrderList
│   ├── stores/           # userStore.js, cartStore.js
│   ├── router/           # index.jsx
│   ├── App.jsx
│   ├── App.css
│   └── main.jsx
├── .env.development
├── .env.production
├── vite.config.js
├── package.json
└── README.md             # 启动步骤、接口说明、截图
```

---

## 8. 学完后你应该能做哪些事

- [ ] 用 `npm create vite@latest` 创建 React 18 + Vite 项目
- [ ] 用 **函数组件 + Hooks** 写页面，props 正确传递
- [ ] 配置 React Router v6：动态路由、嵌套路由、ProtectedRoute 鉴权
- [ ] 用 Zustand 管理用户登录态与购物车，可选 persist
- [ ] 封装 Axios：请求/响应拦截器、token、401 统一处理
- [ ] 用 Ant Design 搭列表、表单、布局
- [ ] 配置 `.env`、Vite proxy、`npm run build` 并 Nginx 部署 SPA
- [ ] 与 Spring Boot 联调，能讲清开发 proxy 与生产反代区别
- [ ] 做一个能演示、能讲 15～20 分钟的 **shop-react** 项目
- [ ] 回答 React 虚拟 DOM、useState、Zustand vs Redux 等高频面试题

---

## 9. 常见问题 FAQ

### Q1：要先学完前端 HTML/CSS/JS 再学 React 吗？

**建议**至少完成 HTML CSS JS 的 JS 06～09（ES6、异步、模块化）再开始 React 01。HTML/CSS 基础可在 React 项目中边做边补。

### Q2：React 18 和 React 19 怎么选？

本资料以 **React 18** 为主线（生态最稳、资料最多）。React 19 新增 Actions、use 等 API，01 章会简要说明差异；新项目用 18 或 19 均可，Vite 模板默认较新版本。

### Q3：Vue 和 React 选哪个？

国内 **Vue 岗位仍很多**；React 在全球与大厂业务线同样主流。本资料走 React 线；若你已学 [Vue](../Vue/00-学习路线图与说明.md)，对照表可加速迁移。

### Q4：Redux 还要学吗？

中小型项目 **Zustand 足够**（本资料默认）。面试可能问 Redux，见 13 章对比；大型老项目可能仍用 Redux Toolkit。

### Q5：跨域怎么办？

- **开发**：Vite `server.proxy` 把 `/api` 转到 8080
- **生产**：Nginx 同域反代 `/api`，或后端 CORS
- 详见 08 章、10 章

### Q6：build 后白屏？

常见原因：`base` 路径错误、资源 404、路由 BrowserRouter 未配 `try_files`。见 10 章排错表。

### Q7：只做前端够吗？

初级 **前端岗** 有 React 项目即可；**全栈/实习** 建议前后端都能 demo。shop-react + 后端 demo 是最佳组合。

### Q8：TypeScript 要学吗？

Vite 可选 TypeScript 模板。初学可先用 **JavaScript**，熟练后再迁 TS；面试初级不强制 TS。

### Q9：和 Next.js？

本资料是 **Vite SPA**。Next.js 是 React 全栈框架（SSR/SSG），就业加分但概念更多；学完 11 章后可自学 Next。

### Q10：Class 组件还要学吗？

新项目 **只用函数组件 + Hooks**。面试可能问 Class 与 Hooks 区别，见 12、13 章。

---

## 10. 与后端学习路线对齐建议

若并行学习后端，推荐对齐节奏（**Java 或 Python 选一条**）：

| 时间 | 前端 React | 后端 Java | 后端 Python |
|------|------------|-----------|-------------|
| 第 1～2 月 | 01～07 | 01～05 | 01～05 |
| 第 2～3 月 | 08～10 + shop-react | 06～09 + demo 扩展 | 06～09 + demo-api 扩展 |
| 第 3 月 | 11 项目 + 13 面试 | 10 项目 + 14 场景题 | 10 项目 + 14 场景题 |
| 冲刺 | 14 总表 + 前端 demo | 15 总表 + 后端 demo | 15 总表 + 后端 demo |

路线图入口：[Java 00](../../后端学习/Java/00-学习路线图与说明.md) · [Python 00](../../后端学习/Python/00-学习路线图与说明.md)

**联调黄金窗口**：后端 04 章有 CRUD 接口后，前端 08 章即可对接；后端 10 章与前端 11 章同一周完成 MVP 最佳。

---

## 11. 与 Vue 学习路线对照（可选双线）

若你同时维护 shop-vue 与 shop-react，建议**同周做同一业务能力**：

| 周 | Vue 章节 | React 章节 | 同一功能 |
|----|----------|------------|----------|
| 1 | 01～02 | 01～02 | 列表页 |
| 2 | 03～04 | 03～04 | 搜索 + 拆组件 |
| 3 | 05～06 | 05～06 | Router |
| 4 | 07～08 | 07～08 | 状态 + 联调 |

Vue 资料入口：[Vue 00 学习路线图](../Vue/00-学习路线图与说明.md)

---

## 12. 文档索引速查

| 编号 | 文件名 | 一句话 | 篇幅 |
|------|--------|--------|------|
| 00 | 学习路线图与说明 | 怎么学、顺序、环境 | 入门 |
| 01 | React 入门与环境搭建 | 第一个 React 项目 | 详 |
| 02 | JSX 语法与组件基础 | JSX 与函数组件 | 详 |
| 03 | State 与事件处理 | useState、表单 | 详 |
| 04 | 组件通信与组件设计 | props、组合 | 详 |
| 05 | Hooks 深入 | useEffect、自定义 Hook | 详 |
| 06 | React Router 路由管理 | 单页路由 | 详 |
| 07 | Zustand 状态管理 | 全局状态 | 详 |
| 08 | Axios 网络请求与前后端联调 | 调后端 | 详 |
| 09 | Ant Design 与 UI 工程化 | 组件库 | 详 |
| 10 | Vite 构建与项目部署 | 打包上线 | 详 |
| 11 | React 项目实战与面试准备 | 商城 MVP | 详 |
| 12 | React 进阶特性 | 性能、并发 | 详 |
| 13 | 高频场景题与面试专题 | 面试问答 | 详 |
| 14 | 补充知识点总表 | 复习索引 | 索引 |

---

## 13. 面试准备路线（13～14 章配合）

| 阶段 | 动作 |
|------|------|
| 项目完成后 | 写 1 分钟 / 3 分钟项目介绍（11 章模板） |
| 每周 | 从 13 章抽 5 题口述 |
| 面试前 1 周 | 14 章总表逐项 ⬜→✅ |
| 面试前 1 天 | shop-react demo 录屏 + 13 章速记 |

---

## 14. 我的笔记区

```text
学习开始日期：
当前进度（编号）：
shop-react Git 地址：
薄弱点（章节）：
与后端联调进度：
与 Vue 对照学习（是/否）：
下周计划：
```

---

## 15. 学习心态

- React 不难在 JSX 语法，难在 **组件化思维**、**状态放在哪**、**useEffect 依赖**
- 每章都要在 shop-react 里 **留痕**，否则 11 章会从头补项目
- 遇到报错先看终端和浏览器 Console，10 章、各章「常见报错表」是第二参考
- **一个能讲清楚的项目** 比背 100 个 API 更有用

---

## 16. 常见环境报错（入门阶段）

| 报错 / 现象 | 原因 | 处理 |
|-------------|------|------|
| `'npm' 不是内部或外部命令` | Node 未装或未加 Path | 重装 Node LTS，勾选 PATH |
| `npm ERR! ERESOLVE` | 依赖版本冲突 | `npm install --legacy-peer-deps` 或换 Node 20 |
| 端口 5173 被占用 | 上次 dev 未关 | 换端口 `npm run dev -- --port 5174` 或杀进程 |
| JSX 报红但可运行 | 缺 eslint 或文件扩展名 | 确保组件文件为 `.jsx` |
| 页面空白无报错 | main.jsx 未 render | 检查 `#root` 与 `createRoot` |
| 中文乱码 | 文件非 UTF-8 | VS Code 右下角改 UTF-8 |
| create-vite 卡住 | 网络 | 换 npm 镜像 `npm config set registry https://registry.npmmirror.com` |
| `React is not defined` | 旧 JSX 转换需显式 import | Vite 默认自动 JSX，检查 vite.config |

---

## 17. 每日 / 每周学习记录模板

### 17.1 日复盘（5 分钟）

```text
日期：
今日章节：
敲了哪些 demo：
shop-react 改动：
遇到的问题与解决：
明日计划：
```

### 17.2 周复盘（15 分钟）

```text
第 __ 周（对应 11 章 W__）：
完成章节：
shop-react 功能增量：
Git commit 数：
是否达到本周验收（是/否）：
下周目标：
```

---

## 18. 与实习 / 校招岗位匹配度

| 岗位 JD 关键词 | 本资料覆盖章节 | 项目体现 |
|----------------|----------------|----------|
| React / 前端框架 | 01～07 | shop-react 全栈 |
| 组件化开发 | 02～04 | ProductCard 等 |
| 前端工程化 | 08～10 | Vite、env、Nginx |
| Ant Design | 09 | 表单表格 Layout |
| 前后端分离 | 08、11 | Axios + Spring Boot |
| 了解 Node | 01、10 | npm、build |
| Next.js / SSR | 未覆盖 | 需另学 |

投递简历时：**JD 写 Vue 也可投全栈**，说明同时维护 shop-vue / shop-react 对照学习。

---

## 19. 推荐扩展资源（选看）

| 类型 | 资源 | 用途 |
|------|------|------|
| 官方文档 | zh-hans.react.dev | API 查阅（中文） |
| 官方文档 | reactrouter.com | 路由 |
| 官方文档 | zustand.docs.pmnd.rs | 状态 |
| 官方文档 | vitejs.dev / ant.design | 构建 UI |
| 对照 | [Vue 学习资料](../Vue/00-学习路线图与说明.md) | 横向对比 |
| 联调 | 后端 demo Swagger | 08～11 章 |

**原则**：以本资料 + shop-react 为主，官方文档作字典，避免同时开 3 套视频教程。

---

## 20. 全栈联调时间线（与后端 00 章对照）

```text
月份 1    React 01～05 + 后端 Java 01～04
          shop-react 本地假数据页面

月份 2    React 06～09 + 后端 05～08
          shop-react 接 MySQL 接口

月份 3    React 10～11 + 后端 09～10
          部署 + 双端项目面试准备

冲刺      React 13～14 + 后端 14～15
          场景题 + 总表自评
```

---

## 21. 能力自检问卷（11 章前做一次）

1. 能否不用文档写出带 `map` + 受控 input 的列表+搜索？  
2. 能否 10 分钟内新建 Router 三页面并切换？  
3. 能否解释 props 为什么不能直接改？  
4. 能否写一个 Zustand store 并在两个页面使用？  
5. 能否配置 Axios 拦截器并调通 GET 接口？  

**5 题中 ≥4 题「能」** 再进入 11 章综合项目；否则回到对应章节补练习。

---

## 22. React 与 Vue 核心概念映射表

学完 React 01～03 后，可用下表与 [Vue 01～03](../Vue/00-学习路线图与说明.md) 对照复习：

| 概念 | Vue 3 | React 18 |
|------|-------|----------|
| 项目脚手架 | create-vue | create vite --template react |
| 根挂载 | `#app` + createApp | `#root` + createRoot |
| 视图层 | template 模板 | JSX return |
| 局部状态 | ref / reactive | useState / useReducer |
| 派生数据 | computed | useMemo |
| 副作用 | watch / onMounted | useEffect |
| 双向绑定 | v-model | 受控 value + onChange |
| 条件渲染 | v-if / v-show | && / 三元 / className |
| 列表 | v-for :key | map + key |
| 父子通信 | props + emit | props + 回调函数 |
| 插槽 | slot | children |
| 全局状态 | Pinia | Zustand |
| 路由 | Vue Router | React Router |
| UI 库 | Element Plus | Ant Design |
| 样式隔离 | scoped | CSS Modules / 命名 |

**迁移建议**：先在同一周完成「同一业务能力」（如商品列表 + 搜索），再对比写法差异，比交替跳章更高效。

---

## 23. 第一章起步检查清单（Day 1）

开始 01 章当天，按顺序打勾：

```text
□ 安装 Node.js LTS，node -v / npm -v 正常
□ 安装 VS Code 插件（ES7 React snippets 等）
□ 安装 Chrome React Developer Tools
□ npm config 镜像（国内网络）
□ npm create vite@latest shop-react -- --template react
□ npm install && npm run dev 看到 5173 页面
□ git init 并完成第一次 commit
□ 阅读 01 章「本章与上一章的关系」
□ 手敲 App.jsx 计数器并成功交互
□ 在 DevTools Components 面板找到 App 与 useState
```

全部完成后，再进入 02 章 JSX；**不要**跳过 01 章计数器 demo。

---

## 24. 常见学习路径误区

| 误区 | 为什么有害 | 正确做法 |
|------|------------|----------|
| 只看不敲 | 面试写不出 JSX | 每章完整手敲 demo |
| 每章新建项目 | 11 章无法集成 | 始终维护 shop-react |
| 过早学 Redux | 复杂度高、挫败 | 先 Zustand（07 章） |
| 跳过 03 表单 | 08 章登录对接困难 | 03 章 LoginForm 必做 |
| 不做后端联调 | 简历缺全栈亮点 | 08 章与后端 04 对齐 |
| React/Vue 同时开新项目 | 精力分散 | 对照表迁移，或先精通一个 |

---

祝你学习顺利。**React 能力 = 组件化思维 + Hooks + 路由与状态 + Axios 联调 + 一个能部署的 shop-react 项目。**
