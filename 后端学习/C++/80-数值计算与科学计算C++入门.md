# 数值计算与科学计算 C++ 入门

> **文件编码**：UTF-8。C++17 + 可选 Eigen。
> **定位**：IEEE754 深入、矩阵运算、迭代法(雅可比/高斯)、误差分析；与 LLMInfra 数学互补。
> **交叉阅读**：[LLMInfra 01 线性代数](../LLMInfra/01-线性代数与数值计算基础.md)、[06 模板](06-模板与泛型编程.md)、[18 高性能](18-高性能C++与内存对齐.md)。

---

## 0. 读前导读（§0）

### 0.1 用一句话弄懂本章

理解 **浮点误差** 与 **线性系统数值解**，读推理引擎中的矩阵运算不犯概念错误。

### 0.2 你需要提前知道什么

| 状态 | 动作 |
|------|------|
| LLMInfra 01 向量矩阵 | 本章 C++ 实现 |
| 高中线性代数 | 够用 |

### 0.3 本章知识地图（☐→☑）

- ☐ IEEE754 结构
- ☐ 手写 matmul
- ☐ 雅可比/高斯迭代
- ☐ 误差阶分析

### 0.4 建议学习时长

**7～10 天**

### 0.5 学完你能做什么

评估数值稳定性；读 BLAS/GEMM 在 Infra 中的角色。

---

## 1. IEEE 754 深入

| 部分 | 位数(float) | 含义 |
|---|---|---|
| 符号 | 1 | 正负 |
| 指数 | 8 | 偏移 127 |
| 尾数 | 23 | 隐含前导 1 |

**机器 epsilon**：使 1+ε≠1 的最小 ε。
**灾难性抵消**：相近大数相减损失有效位。

```cpp
#include <limits>
float eps = std::numeric_limits<float>::epsilon();
bool eq(float a, float b) { return std::fabs(a-b) <= 1e-6f * std::max(1.f, std::fabs(a)); }
```

## 2. 矩阵运算

```cpp
void matmul(const std::vector<double>& A, const std::vector<double>& B,
            std::vector<double>& C, int n) {
    for (int i = 0; i < n; ++i)
        for (int k = 0; k < n; ++k)
            for (int j = 0; j < n; ++j)
                C[i*n+j] += A[i*n+k] * B[k*n+j];
}
```

## 3. 高斯消元

```cpp
bool gaussianElim(std::vector<std::vector<double>>& A, std::vector<double>& b) {
    int n = (int)A.size();
    for (int col = 0; col < n; ++col) {
        int piv = col;
        for (int r = col+1; r < n; ++r) if (std::fabs(A[r][col]) > std::fabs(A[piv][col])) piv = r;
        std::swap(A[col], A[piv]); std::swap(b[col], b[piv]);
        for (int r = col+1; r < n; ++r) {
            double f = A[r][col] / A[col][col];
            for (int c = col; c < n; ++c) A[r][c] -= f * A[col][c];
            b[r] -= f * b[col];
        }
    }
    return true; // 回代略
}
```

## 4. 雅可比迭代

```cpp
std::vector<double> jacobi(const std::vector<std::vector<double>>& A,
                           const std::vector<double>& b, int maxIter, double tol) {
    int n = (int)b.size();
    std::vector<double> x(n), xnew(n);
    for (int it = 0; it < maxIter; ++it) {
        for (int i = 0; i < n; ++i) {
            double s = b[i];
            for (int j = 0; j < n; ++j) if (j != i) s -= A[i][j]*x[j];
            xnew[i] = s / A[i][i];
        }
        if (/* norm(xnew-x) < tol */) break;
        x.swap(xnew);
    }
    return x;
}
```

## 5. 误差分析

前向误差：计算值与精确值差。后向误差：输入扰动使结果精确。
条件数 κ(A)=‖A‖‖A⁻¹‖ 大 → 问题 ill-conditioned。

## 6. 数值专题 1

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_6();
```

## 7. 数值专题 2

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_7();
```

## 8. 数值专题 3

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_8();
```

## 9. 数值专题 4

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_9();
```

## 10. 数值专题 5

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_10();
```

## 11. 数值专题 6

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_11();
```

## 12. 数值专题 7

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_12();
```

## 13. 数值专题 8

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_13();
```

## 14. 数值专题 9

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_14();
```

## 15. 数值专题 10

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_15();
```

## 16. 数值专题 11

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_16();
```

## 17. 数值专题 12

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_17();
```

## 18. 数值专题 13

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_18();
```

## 19. 数值专题 14

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_19();
```

## 20. 数值专题 15

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_20();
```

## 21. 数值专题 16

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_21();
```

## 22. 数值专题 17

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_22();
```

## 23. 数值专题 18

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_23();
```

## 24. 数值专题 19

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_24();
```

## 25. 数值专题 20

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_25();
```

## 26. 数值专题 21

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_26();
```

## 27. 数值专题 22

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_27();
```

## 28. 数值专题 23

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_28();
```

## 29. 数值专题 24

| 方法 | 复杂度 | 稳定 |
|---|---|---|
| GEMM | O(n³) | 依赖实现 |
| LU | O(n³) | 需 pivot |
| Jacobi | O(k n²) | 收敛条件 |

```cpp
void numeric_lab_29();
```


## 6. LU 分解与回代
```cpp
bool luDecomp(std::vector<std::vector<double>>& A, std::vector<int>& piv) {
    int n = (int)A.size();
    piv.resize(n);
    for (int i = 0; i < n; ++i) piv[i] = i;
    for (int k = 0; k < n; ++k) {
        int p = k;
        for (int i = k+1; i < n; ++i)
            if (std::fabs(A[i][k]) > std::fabs(A[p][k])) p = i;
        if (p != k) { std::swap(A[k], A[p]); std::swap(piv[k], piv[p]); }
        if (std::fabs(A[k][k]) < 1e-12) return false;
        for (int i = k+1; i < n; ++i) {
            A[i][k] /= A[k][k];
            for (int j = k+1; j < n; ++j) A[i][j] -= A[i][k] * A[k][j];
        }
    }
    return true;
}
```

## 7. 高斯-赛德尔迭代
```cpp
std::vector<double> gaussSeidel(const std::vector<std::vector<double>>& A,
                                const std::vector<double>& b, int maxIt, double tol) {
    int n = (int)b.size();
    std::vector<double> x(n);
    for (int it = 0; it < maxIt; ++it) {
        double err = 0;
        for (int i = 0; i < n; ++i) {
            double s = b[i];
            for (int j = 0; j < n; ++j) if (j != i) s -= A[i][j] * x[j];
            double nx = s / A[i][i];
            err = std::max(err, std::fabs(nx - x[i]));
            x[i] = nx;
        }
        if (err < tol) break;
    }
    return x;
}
```

## 8. 矩阵范数与条件数
||A||_∞ = max_i Σ_j |a_ij|；κ ≈ ||A|| · ||A^{-1}||。

## 9. 与 [LLMInfra 01](../LLMInfra/01-线性代数与数值计算基础.md) 映射
GEMM→Attention QK^T；softmax 数值稳定→减 max；混合精度→FP16 累加 FP32。

## 10. SIMD/BLAS 注记
工程用 OpenBLAS/MKL/Eigen；本章手写理解 cache 与 pivot。

## 11～28. 数值专题
- 浮点 associativity 非结合
- Kahan summation
- 病态 Hilbert 矩阵
- 迭代收敛谱半径
- Newton 法求 sqrt
- 梯形/ Simpson 积分
- RK4 ODE 入门
- 误差向前向后分析
- 对齐与 SIMD load
- denormal 性能陷阱

## 常见问题 FAQ

1. **float 相等？** 用 epsilon 比较，勿 ==
2. **与 LLMInfra 01？** 01 数学；80 C++ 实现+误差
3. **Eigen 要学吗？** 工程推荐；本章手写理解
4. **高斯 pivot？** 防除零与降误差
5. **Jacobi 收敛？** 对角占优充分非必要
6. **NaN 传播？** 任何运算 NaN 仍为 NaN
7. **矩阵存储？** 行主序 vs 列主序影响 cache
8. **BLAS 层级？** Level1/2/3 向量矩阵乘
9. **混合精度？** FP16 训练 FP32 累加
10. **条件数大？** 换正则化或 SVD

---

## 闭卷自测

1. float 指数偏移？
2. 机器 epsilon？
3. 灾难性抵消？
4. GEMM 复杂度？
5. pivot 作用？
6. Jacobi 更新式？
7. 条件数含义？
8. 与 LLMInfra？
9. NaN==NaN?
10. 行主序好处？

### 自测参考答案

1. 127
2. 1+ε≠1 最小 ε
3. 相近大数相减
4. O(n³)
5. 选主元稳
6. x_i=(b_i-Σ)/a_ii
7. 输入扰动敏感度
8. 01 理论 80 代码
9. false
10. cache 友好


## 80 补充专题 1

深入理解本章与相邻章节的工程权衡（专题 1）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-1
namespace sup { void drill_1() {} }
```

## 80 补充专题 2

深入理解本章与相邻章节的工程权衡（专题 2）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-2
namespace sup { void drill_2() {} }
```

## 80 补充专题 3

深入理解本章与相邻章节的工程权衡（专题 3）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-3
namespace sup { void drill_3() {} }
```

## 80 补充专题 4

深入理解本章与相邻章节的工程权衡（专题 4）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-4
namespace sup { void drill_4() {} }
```

## 80 补充专题 5

深入理解本章与相邻章节的工程权衡（专题 5）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-5
namespace sup { void drill_5() {} }
```

## 80 补充专题 6

深入理解本章与相邻章节的工程权衡（专题 6）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-6
namespace sup { void drill_6() {} }
```

## 80 补充专题 7

深入理解本章与相邻章节的工程权衡（专题 7）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-7
namespace sup { void drill_7() {} }
```

## 80 补充专题 8

深入理解本章与相邻章节的工程权衡（专题 8）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-8
namespace sup { void drill_8() {} }
```

## 80 补充专题 9

深入理解本章与相邻章节的工程权衡（专题 9）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-9
namespace sup { void drill_9() {} }
```

## 80 补充专题 10

深入理解本章与相邻章节的工程权衡（专题 10）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-10
namespace sup { void drill_10() {} }
```

## 80 补充专题 11

深入理解本章与相邻章节的工程权衡（专题 11）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-11
namespace sup { void drill_11() {} }
```

## 80 补充专题 12

深入理解本章与相邻章节的工程权衡（专题 12）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-12
namespace sup { void drill_12() {} }
```

## 80 补充专题 13

深入理解本章与相邻章节的工程权衡（专题 13）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-13
namespace sup { void drill_13() {} }
```

## 80 补充专题 14

深入理解本章与相邻章节的工程权衡（专题 14）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-14
namespace sup { void drill_14() {} }
```

## 80 补充专题 15

深入理解本章与相邻章节的工程权衡（专题 15）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-15
namespace sup { void drill_15() {} }
```

## 80 补充专题 16

深入理解本章与相邻章节的工程权衡（专题 16）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-16
namespace sup { void drill_16() {} }
```

## 80 补充专题 17

深入理解本章与相邻章节的工程权衡（专题 17）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-17
namespace sup { void drill_17() {} }
```

## 80 补充专题 18

深入理解本章与相邻章节的工程权衡（专题 18）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-18
namespace sup { void drill_18() {} }
```

## 80 补充专题 19

深入理解本章与相邻章节的工程权衡（专题 19）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-19
namespace sup { void drill_19() {} }
```

## 80 补充专题 20

深入理解本章与相邻章节的工程权衡（专题 20）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-20
namespace sup { void drill_20() {} }
```

## 80 补充专题 21

深入理解本章与相邻章节的工程权衡（专题 21）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-21
namespace sup { void drill_21() {} }
```

## 80 补充专题 22

深入理解本章与相邻章节的工程权衡（专题 22）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-22
namespace sup { void drill_22() {} }
```

## 80 补充专题 23

深入理解本章与相邻章节的工程权衡（专题 23）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-23
namespace sup { void drill_23() {} }
```

## 80 补充专题 24

深入理解本章与相邻章节的工程权衡（专题 24）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-24
namespace sup { void drill_24() {} }
```

## 80 补充专题 25

深入理解本章与相邻章节的工程权衡（专题 25）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-25
namespace sup { void drill_25() {} }
```

## 80 补充专题 26

深入理解本章与相邻章节的工程权衡（专题 26）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-26
namespace sup { void drill_26() {} }
```

## 80 补充专题 27

深入理解本章与相邻章节的工程权衡（专题 27）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-27
namespace sup { void drill_27() {} }
```

## 80 补充专题 28

深入理解本章与相邻章节的工程权衡（专题 28）。

| 要点 | 说明 |
|------|------|
| 面试 | 口述定义+复杂度+适用场景 |
| 代码 | 对照 examples/ 编译验证 |

```cpp
// supplement 80-28
namespace sup { void drill_28() {} }
```

## 下一章预告

下一章：[返回 C++ 学习路线图](00-学习路线图与说明.md)

---

*下一章：返回 C++ 学习路线图*
