# Linux 学习库验证报告

> 最终验证日期：2026-07-15。统计结果来自同目录脚本和补充静态检查；以后内容有改动时，请重新运行脚本。

```powershell
cd F:\study\后端学习\Linux\99-工具与验证
.\verify_linux.ps1 -Run
```

## 验证范围

- 核心文件是否齐全，旧平铺文件是否已移除。
- Markdown 是否为严格 UTF-8、是否只有一个 H1、代码围栏是否闭合。
- 本地相对链接是否存在。
- 是否残留 notehub、Spring Boot、失效 `todo.md`、全局 MySQL 授权等禁用模式。
- Go 示例是否经过 gofmt，是否能通过 `go test ./...`。
- 有 WSL 时，Shell 脚本是否通过 `bash -n`。
- 有 Docker CLI 时，Compose 是否能通过 `docker compose config`。

## 最终结果

结论：**通过**。

- 核心文档：12 份（`README.md` + 00～10），共 10,844 行。
- 全目录 Markdown：18 份，共 11,721 行。
- 代码围栏标记：1,334 个，全部闭合。
- Linux 链接：目录内 20 个、目录外 34 个，共 54 个，失效 0。
- Go / Java / 短链关键词：339 / 1 / 838；主线已切换为 Go 短链。
- 可运行 Go 文件：3 个；`gofmt` 与 `go test ./...` 均通过。
- Shell 脚本：3 个；已使用 `shfmt v3.11.0` 成功解析和格式化。
- 部署配置样例：7 个，包括 Compose、Dockerfile、systemd、环境变量与两份 Nginx 配置。
- 全目录文件严格 UTF-8 解码：35 个文件，错误 0。
- 旧版 00～15 平铺文档及其路径引用：残留 0。
- 自动验证脚本：错误 0，环境限制警告 2。

## 环境限制

- 当前 Windows 主机没有可用的 WSL 发行版，因此脚本跳过真实 `bash -n`；三个 Shell 脚本已由 `shfmt v3.11.0` 补充完成语法解析。
- 当前主机没有 Docker CLI，因此脚本跳过真实 `docker compose config`；`compose.yaml` 已通过 PyYAML 静态解析。
- systemd 与 Nginx 无法在当前 Windows 主机真实启动，已检查 systemd 必需字段、Nginx 花括号和 upstream 配置；上线实验仍应在 Ubuntu 环境再执行一次真实验证。

## 复验命令

```powershell
cd F:\study\后端学习\Linux\99-工具与验证
.\verify_linux.ps1 -Run
```
